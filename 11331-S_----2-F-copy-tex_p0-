### 11331:S --- 2/F/copy(tex_p0)
#ifdef GL_ES
precision lowp float;
#endif

varying vec2 v_texCoord;

uniform sampler2D p0;

void main() 
{
  gl_FragColor = texture2D(p0, v_texCoord);

//  gl_FragColor = vec4(0.0,0.5,1.0,0.5);
}
