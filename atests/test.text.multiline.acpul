### 5350 test.text.multiline

document {
 
};

lines.vec {
 dma0 5;
 use {
  # o
  buffer.dma(u0, dma0, _0,0);
 };
 end {
  dma.off(u0, dma0);
 };
 
 select {
  # dma, i
  b0:=dma.get(u0, dma0,_1);
  if (b0==0) {
   
  };
  buffer.dma(u0, _0, b0,0);
 };
};

### 5351 test.text.multiline.draw


