### 3290 Networking
# r0, r1

r0:=k==0;

draw(r0, 3291);


_ @1010;
_ @sys.net;

l0:=net.is.connected(u0);
if (l0)
{
 if (r1==0) {
  r1:=1;
  net.started(u0);
 };
};
if (l0==0)
{
 r1:=0;
};

_ @3299;

if(net.is.setup.event(u0))
{
 gnet.id:=net.get.index(u0);
# gnet.frame:=0;
# gt:=0;
# glt:=0;
 l0:=1234555;
 watch(l0);
 watch(gnet.id);
};


if (glt < 0.2) {
 glt+=dt;
 watch(glt);
 #glt:=0;
 #watch(dt);
};

#if(net.is.sync)
#{
#
#};

k:=1;

### 3291 Networking.draw

#watch(r0);
