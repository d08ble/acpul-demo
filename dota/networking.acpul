### 3290 Networking
# r0, r1

r0:=k==0;

draw(r0, 3291);

s:=0.2;

_ @1010;
_ @sys.net;

l0:=net.is.connected(u0);
if (l0)
{
 if (r1==0) {
  r1:=1;
  net.started(u0);
 }
};
if (l0==0)
{
 r1:=0;
};

_ @3299;

#r2:=net.is.setup();
#if(r2)
#{
# gnet.id:=r2;
# gnet.frame:=0;
# gt:=0;
# glt:=0;
#};

if (glt < 0.2) {
 glt+=dt;
 watch(glt);
 #glt:=0;
 #watch(dt);
};

#if(net.is.sync)
#{
#
#};

k:=1;

### 3291 Networking.draw

#watch(r0);
color(u0, 0,0.5,0,1);
rect(u0, 6, 0,0,1024,768);
