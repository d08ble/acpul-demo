### 3230 Unit

r0:=k==0;

draw(r0, 3231);

_ @1010;
_ @sys.node;

OBJECT 3220;

obj @OBJECT;
ot obj.types;
ost obj.subtypes;

subtype p0;
player.id p1;
bot.id p1;

UNIT.ROBOT 3232;
UNIT.COMPLEX 3234;
if (r0) { #hack!
 if (subtype==ost.bot) {
  node.new.this.ex(r0, UNIT.COMPLEX, bot.id);
 };
 if (subtype==ost.player) {
  node.new.this.ex(r0, UNIT.ROBOT, player.id);
 };
};

k:=1;

### 3231 Unit.draw

#watch(r0);

