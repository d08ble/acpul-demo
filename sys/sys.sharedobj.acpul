### 1039 sys.sharedobj

sharedobj {
 self u55;
 _ @1010;
 _ @sys.obj;
 init {
  # r0
  u55:=map.new(_0, 1024, 1000000);
  it.key.new(_0, u55,0, 0);
  it.dma(_0, 0, u55,0);
  dma.set(_0, 0,0, 55); # set identity - js operator: u55={0:55}
 };
 key {
  # r0, dma, key ->
  if (it.key(_0, u55,_1, _2)>0,
   it.dma(_0, 0, u55,_1),
   -1);
 };
 key.new {
  # r0, dma, key
  it.key.new(_0, u55,_1, _2);
  it.dma(_0, 0, u55,_1);
 };
};